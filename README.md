# Quantum-Circuit-Simulator
Build a quantum circuit simulator from scratch in Python. Implement qubit state vectors, single-qubit gates (H, X, Y, Z, S, T), two-qubit gates (CNOT), and measurement. Visualize circuits and Bloch sphere representations.
